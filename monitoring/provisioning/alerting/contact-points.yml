apiVersion: 1
contactPoints:
  - orgId: 1
    name: webhook-default
    receivers:
      - uid: webhook_default
        type: webhook
        settings:
          url: ${GRAFANA_ALERT_WEBHOOK_URL}
          httpMethod: POST
          sendResolved: true
